server {
    listen 61973 ssl;
    server_name smart.bodora.pl;
    
    # SSL certificates (musisz je dostarczyć)
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    # Frontend - proxy do Vite
    location / {
        proxy_pass http://127.0.0.1:61974;  # Frontend na innym porcie
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
    
    # Backend API - proxy do uvicorn
    location /api/ {
        proxy_pass http://127.0.0.1:38273/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
